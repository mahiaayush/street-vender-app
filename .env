# Database configuration (connecting local backend to native MySQL)
DATABASE_URL="mysql://root:infotech%400193@localhost:3306/street_vender_main"

# Server configuration
PORT=4000
JWT_SECRET="super-secret-vendor-key-123"

# Kafka Event Bus configuration (connecting local backend to Docker Kafka)
USE_KAFKA=true
KAFKA_BROKERS="localhost:9092"
KAFKA_CLIENT_ID="street-vendor-tracker"

# Firebase Realtime DB (disable/mock since we run mock operations locally)
USE_FIREBASE=false
